func (r *{{RepoName}}) {{MethodName}}(ctx context.Context, db util.Executer) (int, error) {
	filter := querybuilder.Get(ctx)
	f := filter.ToSQL(1)

	sql := "SELECT COUNT(*) FROM {{TableName}} WHERE " + f.Where

	var count int
	err := db.QueryRowContext(ctx, sql, f.Args...).Scan(&count)
	return count, err
}
