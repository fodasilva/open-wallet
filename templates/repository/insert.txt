func (r *{{RepoName}}) {{MethodName}}(ctx context.Context, db utils.Executer, data {{PayloadType}}) error {
	query := squirrel.Insert("{{TableName}}").
		PlaceholderFormat(squirrel.Dollar)

{{InColsVals}}

	sql, args, err := query.ToSql()
	if err != nil {
		return err
	}

	_, err = db.ExecContext(ctx, sql, args...)

	return err
}
