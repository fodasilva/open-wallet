func (r *{{RepoName}}) {{MethodName}}(ctx context.Context, db util.Executer, data {{PayloadType}}) error {
	filter := querybuilder.Get(ctx)
{{UpdateSets}}

	f := filter.ToSQL(len(values) + 1)
	sql := "UPDATE {{TableName}} SET " + strings.Join(sets, ", ") + " WHERE " + f.Where
	values = append(values, f.Args...)

	_, err := db.ExecContext(ctx, sql, values...)

	return err
}
